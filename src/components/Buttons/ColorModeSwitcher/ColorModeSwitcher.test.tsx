import * as testing from 'utils/testing';

import { ColorModeSwitcher } from './ColorModeSwitcher';

test('throw away', () => {
	expect(true).toEqual(true);
});

// test('renders the color mode switcher button', () => {
// 	const { getByText } = testing.render(<ColorModeSwitcher />);
// 	const text = getByText(/home page/i);
// 	expect(text).toBeInTheDocument();
// });
